# ENVOY.gemlink
A SIP -> AI voice gateway focused on direct IP calling and real time audio bridging. The app acts as a minimal SIP endpoint: it answers INVITEs, negotiates RTP via SDP, and streams PCMU audio over RTP. The current build is a Windows GUI with a central log console (subject to change later).
